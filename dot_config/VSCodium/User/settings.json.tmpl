{{- /* VSCodium user settings — Linux XDG target (~/.config/VSCodium/User/settings.json).
   The body is ONE shared template (.chezmoitemplates/vscodium-settings.json.tmpl) so Linux
   and macOS (Library/Application Support/VSCodium) render byte-identical settings from the
   same source. */ -}}
{{- includeTemplate "vscodium-settings.json.tmpl" . -}}