Generate a session name as a lowercase Conventional Commit: `<type>: <summary>`.

Allowed types: `feat`, `fix`, `docs`, `style`, `refactor`, `perf`, `test`, `build`, `ci`, `chore`, `revert`.
The summary must be imperative, specific, and have no trailing period.
The whole title must be under 80 characters and under 12 words.
If the message has no concrete task, output exactly `none`.
