# STM32 ROM DFU bootloader access for firmware flashing.
# Matches every STM32 ROM DFU device, not only the Gem80: 0483:df11 is ST's generic bootloader identity.
SUBSYSTEM=="usb", ATTR{idVendor}=="0483", ATTR{idProduct}=="df11", TAG+="uaccess"
