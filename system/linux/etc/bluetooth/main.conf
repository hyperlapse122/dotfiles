[General]
# Advertise Classic BR/EDR alongside LE (dual mode) so A2DP stays available for
# stereo audio — some TWS earbuds otherwise fall back to mono over LE Audio on
# current BlueZ.
ControllerMode = dual

# Experimental BlueZ D-Bus interfaces + experimental kernel features.
Experimental = true
KernelExperimental = true
