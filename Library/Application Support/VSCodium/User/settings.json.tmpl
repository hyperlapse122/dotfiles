{{- /* VSCodium user settings — macOS target (~/Library/Application Support/VSCodium/
   User/settings.json). The body is ONE shared template
   (.chezmoitemplates/vscodium-settings.json.tmpl) so the Linux XDG and Windows targets
   render byte-identical settings from the same source. */ -}}
{{- includeTemplate "vscodium-settings.json.tmpl" . -}}