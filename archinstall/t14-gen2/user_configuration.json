{
  "app_config": {
    "audio_config": {
      "audio": "pipewire"
    },
    "bluetooth_config": {
      "enabled": true
    },
    "power_management_config": {
      "power_management": "power-profiles-daemon"
    }
  },
  "archinstall-language": "English",
  "auth_config": {},
  "bootloader_config": {
    "bootloader": "Systemd-boot",
    "removable": false,
    "uki": true
  },
  "custom_commands": [
    "bash <<'T14_GEN2_ARCHINSTALL'\nset -euo pipefail\n\nUSERNAME=h82\nREPO_URL=https://github.com/hyperlapse122/dotfiles.git\nUSER_HOME=$(getent passwd \"$USERNAME\" | cut -d: -f6)\nDOTFILES_DIR=\"$USER_HOME/dotfiles\"\n\nif [ -z \"$USER_HOME\" ]; then\n  echo \"t14-gen2 post-install: user $USERNAME not found\" >&2\n  exit 1\nfi\n\npacman -Sy --noconfirm --needed git curl sudo base-devel sbctl cryptsetup tpm2-tools efibootmgr\nchmod 1777 /tmp\n\ninstall -Dm440 /dev/stdin /etc/sudoers.d/00-wheel-nopasswd <<<'%wheel ALL=(ALL:ALL) NOPASSWD: ALL'\ninstall -Dm440 /dev/stdin /etc/sudoers.d/01-${USERNAME}-nopasswd <<<\"${USERNAME} ALL=(ALL:ALL) NOPASSWD: ALL\"\n\nfor service in firewalld keyd docker cups systemd-resolved power-profiles-daemon sddm fwupd-refresh.timer; do\n  systemctl --root=/ enable \"$service\" || true\ndone\n\ninstall -Dm644 /dev/stdin /etc/X11/xorg.conf.d/00-keyboard.conf <<'XKB'\nSection \"InputClass\"\n  Identifier \"system-keyboard\"\n  MatchIsKeyboard \"on\"\n  Option \"XkbLayout\" \"kr\"\nEndSection\nXKB\nchsh -s /usr/bin/zsh \"$USERNAME\"\n\nif findmnt -no FSTYPE / | grep -qx btrfs; then\n  if [ ! -e /swap/swapfile ]; then\n    btrfs subvolume create /swap 2>/dev/null || true\n    btrfs filesystem mkswapfile --size 4g --uuid clear /swap/swapfile\n  fi\n  grep -qF '/swap/swapfile none swap defaults 0 0' /etc/fstab \\\n    || printf '%s\\n' '/swap/swapfile none swap defaults 0 0' >> /etc/fstab\nfi\n\ninstall -Dm644 /dev/stdin /etc/sddm.conf.d/10-wayland.conf <<'SDDM_CONF'\n[General]\nDisplayServer=wayland\nGreeterEnvironment=QT_WAYLAND_SHELL_INTEGRATION=layer-shell\n\n[Wayland]\nCompositorCommand=kwin_wayland --drm --no-lockscreen --no-global-shortcuts --locale1\nSDDM_CONF\n\nif [ ! -x \"$USER_HOME/.local/bin/uv\" ]; then\n  runuser -l \"$USERNAME\" -c \"curl -LsSf https://astral.sh/uv/install.sh | sh\"\nfi\n\nif [ ! -d \"$DOTFILES_DIR/.git\" ]; then\n  runuser -l \"$USERNAME\" -c \"git clone $REPO_URL $DOTFILES_DIR\"\nelse\n  runuser -l \"$USERNAME\" -c \"git -C $DOTFILES_DIR pull --ff-only\"\nfi\n\ninstall -Dm755 /dev/stdin /usr/local/sbin/t14-gen2-secureboot-tpm-enroll <<'T14_GEN2_SECUREBOOT_SCRIPT'\n#!/usr/bin/env bash\n# archinstall/t14-gen2/secureboot-tpm-enroll.sh\n#\n# First-boot Secure Boot signing and TPM2 LUKS enrollment for the\n# t14-gen2 Arch install (Lenovo ThinkPad T14 Gen 2 (Intel)).\n#\n# Linux-only by design: installed as a systemd oneshot service by\n# archinstall/t14-gen2/user_configuration.json.\n#\n# Mirror of archinstall/UX5606/secureboot-tpm-enroll.sh — same logic, only\n# STATE_DIR differs. Keep these two in sync when fixing bugs.\n\nset -euo pipefail\n\nSTATE_DIR=/var/lib/t14-gen2-install\nDONE_FILE=\"$STATE_DIR/secureboot-tpm.done\"\n\nlog() {\n  printf 't14-gen2-secureboot-tpm: %s\\n' \"$*\"\n}\n\nread_sysfs_value() {\n  local path=$1\n\n  if [[ -r \"$path\" ]]; then\n    tr -d '\\0' < \"$path\"\n  fi\n}\n\nqemu_uefi_guest() {\n  local product_name sys_vendor virt\n\n  virt=\"$(systemd-detect-virt --vm 2>/dev/null || true)\"\n  sys_vendor=\"$(read_sysfs_value /sys/class/dmi/id/sys_vendor)\"\n  product_name=\"$(read_sysfs_value /sys/class/dmi/id/product_name)\"\n\n  [[ -d /sys/firmware/efi/efivars ]] || return 1\n  [[ \"$virt\" == qemu || \"$virt\" == kvm || \"$sys_vendor\" == QEMU* || \"$product_name\" == *QEMU* ]]\n}\n\nhas_secure_boot_keys() {\n  compgen -G '/var/lib/sbctl/keys/PK/*.key' >/dev/null \\\n    || compgen -G '/usr/share/secureboot/keys/PK/*.key' >/dev/null\n}\n\nfirmware_setup_mode_enabled() {\n  sbctl status 2>/dev/null | grep -Eiq 'Setup Mode:.*Enabled'\n}\n\nenroll_secure_boot_keys() {\n  if sbctl enroll-keys -m -f; then\n    log 'enrolled Secure Boot keys with Microsoft and firmware builtin keys.'\n    return 0\n  fi\n\n  log 'sbctl could not enroll with firmware builtin keys.'\n\n  if qemu_uefi_guest || [[ \"${SBCTL_ALLOW_NO_FIRMWARE_BUILTINS:-0}\" == 1 ]]; then\n    log 'falling back to Microsoft keys without firmware builtin keys.'\n    if sbctl enroll-keys -m; then\n      return 0\n    fi\n  fi\n\n  if qemu_uefi_guest || [[ \"${SBCTL_ALLOW_OWNER_ONLY_ENROLLMENT:-0}\" == 1 ]]; then\n    log 'falling back to owner-only keys with sbctl firmware-risk override.'\n    sbctl enroll-keys --yes-this-might-brick-my-machine\n    return\n  fi\n\n  log 'refusing non-vendor/owner-only Secure Boot enrollment on physical hardware.'\n  log 'inspect missing *Default efivars or opt in manually after confirming OptionROM safety.'\n  return 1\n}\n\nsign_existing_boot_artifacts() {\n  local path\n\n  if ! has_secure_boot_keys; then\n    log 'local sbctl keys are not available; skipping boot artifact signing.'\n    return 0\n  fi\n\n  shopt -s nullglob\n  for path in \\\n    /efi/EFI/systemd/systemd-bootx64.efi \\\n    /boot/EFI/systemd/systemd-bootx64.efi \\\n    /efi/EFI/BOOT/BOOTX64.EFI \\\n    /boot/EFI/BOOT/BOOTX64.EFI \\\n    /efi/EFI/Linux/*.efi \\\n    /boot/EFI/Linux/*.efi; do\n    if [[ -e \"$path\" ]]; then\n      sbctl sign -s \"$path\"\n    fi\n  done\n  shopt -u nullglob\n\n  while IFS= read -r line; do\n    if [[ \"$line\" =~ (/[^[:space:]]+)[[:space:]]+is[[:space:]]+not[[:space:]]+signed ]]; then\n      sbctl sign -s \"${BASH_REMATCH[1]}\"\n    fi\n  done < <(sbctl verify 2>&1 || true)\n}\n\nconfigure_tpm_unlock_boot() {\n  local luks_device luks_uuid mapper_name options root_flags root_fstype console_args\n\n  luks_device=$1\n  luks_uuid=\"$(blkid -s UUID -o value \"$luks_device\")\"\n  mapper_name=\"$(findmnt -no SOURCE / | sed 's|/dev/mapper/||')\"\n  mapper_name=\"${mapper_name%%[*}\"\n\n  if [[ -z \"$luks_uuid\" || -z \"$mapper_name\" ]]; then\n    log 'missing LUKS UUID or mapper name; skipping TPM boot configuration.'\n    return 0\n  fi\n\n  if ! grep -Eq '^HOOKS=.*sd-encrypt' /etc/mkinitcpio.conf; then\n    sed -i 's/^HOOKS=.*/HOOKS=(base systemd autodetect microcode modconf kms keyboard sd-vconsole block sd-encrypt filesystems fsck)/' /etc/mkinitcpio.conf\n    log 'switched mkinitcpio hooks from encrypt to sd-encrypt for TPM2 unlock.'\n  fi\n\n  root_flags=\"$(findmnt -no OPTIONS / | tr ',' '\\n' | grep '^subvol=' | head -n1 || true)\"\n  root_fstype=\"$(findmnt -no FSTYPE / || true)\"\n  console_args=\"$(tr ' ' '\\n' < /etc/kernel/cmdline 2>/dev/null | grep '^console=' | xargs || true)\"\n  options=\"rd.luks.name=$luks_uuid=$mapper_name rd.luks.options=tpm2-device=auto root=/dev/mapper/$mapper_name rw\"\n  if [[ -n \"$root_flags\" ]]; then\n    options=\"$options rootflags=$root_flags\"\n  fi\n  if [[ -n \"$root_fstype\" ]]; then\n    options=\"$options rootfstype=$root_fstype\"\n  fi\n  if [[ -n \"$console_args\" ]]; then\n    options=\"$options $console_args\"\n  fi\n  printf '%s\\n' \"$options\" > /etc/kernel/cmdline\n  log 'updated /etc/kernel/cmdline for systemd TPM2 unlock.'\n\n  mkinitcpio -P\n  sign_existing_boot_artifacts\n}\n\nroot_luks_device() {\n  local mapper source\n\n  source=\"$(findmnt -no SOURCE / || true)\"\n  if [[ \"$source\" == /dev/mapper/* ]]; then\n    mapper=\"${source#/dev/mapper/}\"\n    mapper=\"${mapper%%[*}\"\n    cryptsetup status \"$mapper\" 2>/dev/null \\\n      | awk -F: '/^[[:space:]]*device:/ { gsub(/^[[:space:]]+/, \"\", $2); print $2; exit }'\n    return\n  fi\n\n  if [[ -n \"$source\" ]] && cryptsetup isLuks \"$source\" 2>/dev/null; then\n    printf '%s\\n' \"$source\"\n  fi\n\n  return 0\n}\n\nenroll_tpm_for_root_luks() {\n  local luks_device passphrase passphrase_file\n\n  if [[ ! -e /sys/class/tpm/tpm0 ]]; then\n    log 'TPM2 device not found; skipping TPM enrollment.'\n    return 0\n  fi\n\n  luks_device=\"$(root_luks_device)\"\n  if [[ -z \"$luks_device\" ]]; then\n    log 'root filesystem is not detected as LUKS; skipping TPM enrollment.'\n    return 0\n  fi\n\n  if cryptsetup luksDump \"$luks_device\" | grep -q 'systemd-tpm2'; then\n    log \"TPM2 token already enrolled for $luks_device.\"\n    configure_tpm_unlock_boot \"$luks_device\"\n    return 0\n  fi\n\n  log \"enrolling TPM2 token for $luks_device bound to Secure Boot PCR 7.\"\n  if [[ -r /etc/t14-gen2-luks-passphrase ]]; then\n    passphrase=\"$(head -c 1024 /etc/t14-gen2-luks-passphrase)\"\n    log 'using LUKS passphrase from /etc/t14-gen2-luks-passphrase.'\n  else\n    passphrase=\"$(systemd-ask-password \"LUKS passphrase for TPM2 enrollment of $luks_device\" || true)\"\n  fi\n  if [[ -z \"$passphrase\" ]]; then\n    log 'empty passphrase received; skipping TPM enrollment.'\n    return 0\n  fi\n\n  passphrase_file=\"$(mktemp)\"\n  chmod 600 \"$passphrase_file\"\n  printf '%s' \"$passphrase\" > \"$passphrase_file\"\n  systemd-cryptenroll \"$luks_device\" \\\n    --unlock-key-file=\"$passphrase_file\" \\\n    --tpm2-device=auto \\\n    --tpm2-pcrs=7\n  shred -u \"$passphrase_file\" 2>/dev/null || rm -f \"$passphrase_file\"\n\n  shred -u /etc/t14-gen2-luks-passphrase 2>/dev/null || rm -f /etc/t14-gen2-luks-passphrase\n\n  if ! grep -qF 'tpm2-device=auto' /etc/crypttab 2>/dev/null; then\n    local mapper_name uuid\n    mapper_name=\"$(findmnt -no SOURCE / | sed 's|/dev/mapper/||')\"\n    mapper_name=\"${mapper_name%%[*}\"\n    uuid=\"$(blkid -s UUID -o value \"$luks_device\")\"\n    if [[ -n \"$mapper_name\" && -n \"$uuid\" ]]; then\n      printf '%s\\tUUID=%s\\tnone\\ttpm2-device=auto\\n' \"$mapper_name\" \"$uuid\" >> /etc/crypttab\n      log \"added /etc/crypttab entry: $mapper_name UUID=$uuid tpm2-device=auto\"\n    fi\n  fi\n\n  configure_tpm_unlock_boot \"$luks_device\"\n}\n\nmain() {\n  if [[ \"${EUID}\" -ne 0 ]]; then\n    log 'must run as root.'\n    return 1\n  fi\n\n  mkdir -p \"$STATE_DIR\"\n\n  if [[ -e \"$DONE_FILE\" ]]; then\n    log 'already completed.'\n    return 0\n  fi\n\n  if [[ ! -d /sys/firmware/efi/efivars ]]; then\n    log 'system is not booted via UEFI; Secure Boot setup cannot run here.'\n    return 0\n  fi\n\n  if ! command -v sbctl >/dev/null; then\n    log 'sbctl is not installed; Secure Boot setup cannot run.'\n    return 0\n  fi\n\n  if ! has_secure_boot_keys; then\n    if firmware_setup_mode_enabled; then\n      sbctl create-keys\n    else\n      log 'firmware is not in Setup Mode; skipping key creation/enrollment.'\n      log 'put firmware in Setup Mode, then rerun: sudo systemctl start t14-gen2-secureboot-tpm-enroll.service'\n    fi\n  fi\n\n  if firmware_setup_mode_enabled; then\n    if ! enroll_secure_boot_keys; then\n      sign_existing_boot_artifacts\n      log 'key enrollment incomplete; skipping TPM2 PCR7 enrollment and leaving service pending.'\n      return 0\n    fi\n    sign_existing_boot_artifacts\n    log 'Secure Boot keys enrolled; reboot once before TPM2 PCR7 enrollment.'\n    return 0\n  fi\n\n  sign_existing_boot_artifacts\n  enroll_tpm_for_root_luks\n\n  touch \"$DONE_FILE\"\n  log 'Secure Boot signing and TPM2 enrollment finished.'\n}\n\nmain \"$@\"\nT14_GEN2_SECUREBOOT_SCRIPT\ninstall -Dm644 /dev/stdin /etc/systemd/system/t14-gen2-secureboot-tpm-enroll.service <<'T14_GEN2_SECUREBOOT_SERVICE'\n[Unit]\nDescription=t14-gen2 Secure Boot signing and TPM2 LUKS enrollment\nDocumentation=https://wiki.archlinux.org/title/Unified_Extensible_Firmware_Interface/Secure_Boot#Assisted_process_with_sbctl\nDocumentation=https://wiki.archlinux.org/title/Systemd-cryptenroll#Trusted_Platform_Module\nDocumentation=https://wiki.archlinux.org/title/Lenovo_ThinkPad_T14/T14s_(Intel)_Gen_2\nAfter=local-fs.target systemd-udev-settle.service\nWants=systemd-udev-settle.service\nConditionPathExists=!/var/lib/t14-gen2-install/secureboot-tpm.done\n\n[Service]\nType=oneshot\nExecStart=/usr/local/sbin/t14-gen2-secureboot-tpm-enroll\n\n[Install]\nWantedBy=multi-user.target\nT14_GEN2_SECUREBOOT_SERVICE\nsystemctl --root=/ enable t14-gen2-secureboot-tpm-enroll.service\n\n# Make the installed-system QEMU validation fully serial and unattended. The\n# live ISO is direct-kernel booted on ttyS0, but the installed system boots\n# through UEFI/systemd-boot. Force immediate default boot and rebuild the UKI\n# with serial console output so LUKS prompts are visible in the tmux pane.\nmkdir -p /efi/loader\nprintf '%s\\n' 'timeout 0' 'console-mode keep' > /efi/loader/loader.conf\nif [ -r /etc/kernel/cmdline ] && ! grep -q 'console=ttyS0' /etc/kernel/cmdline; then\n  printf '%s console=ttyS0,115200n8\\n' \"$(cat /etc/kernel/cmdline)\" > /etc/kernel/cmdline\nelif [ ! -e /etc/kernel/cmdline ]; then\n  printf '%s\\n' 'console=ttyS0,115200n8' > /etc/kernel/cmdline\nfi\nmkinitcpio -P\n\nrunuser -l \"$USERNAME\" -c \"bash $DOTFILES_DIR/install.sh -vv --exit-on-failure 2>&1 | tee /tmp/dotfiles-install.log\"\n\nfwupdmgr enable-remote -y lvfs-testing 2>/dev/null || true\n\nprintf 't14-gen2 post-install: complete\\n'\nT14_GEN2_ARCHINSTALL"
  ],
  "hostname": "t14-gen2",
  "kernels": [
    "linux-zen"
  ],
  "locale_config": {
    "kb_layout": "us",
    "sys_enc": "UTF-8",
    "sys_lang": "ko_KR.UTF-8"
  },
  "mirror_config": {
    "custom_repositories": [],
    "custom_servers": [],
    "mirror_regions": {
      "Worldwide": [
        "https://geo.mirror.pkgbuild.com/$repo/os/$arch"
      ]
    },
    "optional_repositories": [
      "multilib"
    ]
  },
  "network_config": {
    "type": "nm"
  },
  "ntp": true,
  "packages": [
    "base",
    "base-devel",
    "intel-ucode",
    "linux-zen",
    "linux-zen-headers",
    "linux-firmware",
    "linux-firmware-intel",
    "linux-firmware-realtek",
    "linux-firmware-other",
    "linux-firmware-mediatek",
    "linux-firmware-marvell",
    "linux-firmware-mellanox",
    "linux-firmware-qcom",
    "linux-firmware-qlogic",
    "linux-firmware-broadcom",
    "linux-firmware-cirrus",
    "linux-firmware-atheros",
    "linux-firmware-liquidio",
    "linux-firmware-nfp",
    "linux-firmware-whence",
    "sof-firmware",
    "mesa",
    "vulkan-intel",
    "intel-media-driver",
    "libva-intel-driver",
    "lib32-vulkan-intel",
    "lib32-mesa",
    "fwupd",
    "fwupd-efi",
    "brightnessctl",
    "fprintd",
    "thermald",
    "bash",
    "bash-completion",
    "bash-language-server",
    "bind",
    "cairo",
    "cargo",
    "cmake",
    "clang",
    "cryptsetup",
    "cups",
    "cups-pdf",
    "docker",
    "docker-buildx",
    "docker-compose",
    "efibootmgr",
    "fcitx5",
    "fcitx5-hangul",
    "firefox-developer-edition",
    "firefox-developer-edition-i18n-ko",
    "firewalld",
    "fluidsynth",
    "foomatic-db",
    "foomatic-db-engine",
    "foomatic-db-ppds",
    "geoclue",
    "geocode-glib-2",
    "ghostscript",
    "git",
    "git-lfs",
    "git-zsh-completion",
    "github-cli",
    "glab",
    "gnupg",
    "gsfonts",
    "gtk3",
    "gtk4",
    "keyd",
    "kleopatra",
    "layer-shell-qt",
    "less",
    "libxcb",
    "llvm",
    "man",
    "man-db",
    "mise",
    "noto-fonts",
    "noto-fonts-cjk",
    "noto-fonts-emoji",
    "noto-fonts-extra",
    "obsidian",
    "openssh",
    "polkit",
    "powerdevil",
    "power-profiles-daemon",
    "plymouth",
    "plymouth-kcm",
    "qt5-base",
    "qt6-base",
    "rust",
    "screen",
    "sbctl",
    "shellcheck",
    "speech-dispatcher",
    "steam",
    "systemd",
    "system-config-printer",
    "usage",
    "trash-cli",
    "tree",
    "tailscale",
    "telegram-desktop",
    "tmux",
    "tpm2-tools",
    "ttf-jetbrains-mono",
    "ttf-jetbrains-mono-nerd",
    "webkit2gtk-4.1",
    "webkitgtk-6.0",
    "xdg-desktop-portal",
    "zsh",
    "zsh-autocomplete",
    "zsh-autosuggestions",
    "zsh-completions",
    "zsh-history-substring-search",
    "zsh-syntax-highlighting",
    "zshdb"
  ],
  "parallel_downloads": 5,
  "profile_config": {
    "gfx_driver": "Intel (open-source)",
    "greeter": "sddm",
    "profile": {
      "custom_settings": {
        "KDE Plasma": {}
      },
      "details": [
        "KDE Plasma"
      ],
      "main": "Desktop"
    }
  },
  "script": null,
  "services": [
    "cups",
    "docker",
    "firewalld",
    "fwupd-refresh.timer",
    "keyd",
    "power-profiles-daemon",
    "sddm",
    "systemd-resolved",
    "thermald"
  ],
  "swap": false,
  "timezone": "Asia/Seoul",
  "version": "3.0.14"
}
