# Package manifest for the native Linux installers.
# (.chezmoiscripts/20-linux-fedora/run_onchange_before_fedora.sh.tmpl).
# Fedora and Ubuntu are managed Linux distributions.
#
# Single source of truth (like system.yaml): add a package, repo, flatpak, or
# dotnet tool HERE, not in the installer. Every list is rendered into the
# installer, so an edit re-triggers the run_onchange_ script automatically.
#
# NAMING — every key this repo owns is camelCase, across all of .chezmoidata.
# The ONE exception here is the arch keys of a directRpms entry's
# `rpms:` map (`x86_64`, `aarch64`): those are kernel arch identifiers,
# matched verbatim against `uname -m` output by the installer. They are upstream
# spellings, not schema keys — camelCasing them would simply break the lookup.
#
# ---------------------------------------------------------------------------
# `gates:` — which named host fact decides each CONDITIONAL package group.
#
# Most keys below install unconditionally. Conditional groups are listed in the
# `gates:` map, which binds each group key to a fact from the host-fact registry
# (.chezmoidata/facts.yaml). The value is validated before it reaches the
# installer, so an unknown fact cannot silently evaluate false.
#
# Gate-expression grammar — the same one system.yaml's `gate:` uses, checked by
# the same partial (.chezmoitemplates/facts-validate.tmpl):
#
#   <fact>          boolean fact is true       nvidia, virt
#   !<fact>         boolean fact is false      "!virt"  — MUST be YAML-quoted:
#                                              a bare `!` opens a YAML tag
#   <fact>.<value>  string fact equals value   desktop.kde, desktop.gnome
#
# THE MAP IS NOW LOAD-BEARING — it drives the flag, not just names it.
# The installer renders the whole fact set into bash (FACT_* via
# .chezmoitemplates/facts-sh.tmpl), then seeds its HAS_* / IS_* flags by
# running EACH KEY'S OWN VALUE from this map through the shared fact_gate()
# dispatcher (.chezmoitemplates/facts-gate.sh.tmpl):
#
#   HAS_NVIDIA=0; fact_gate {{ index $gates "nvidiaPackages" | quote }} && HAS_NVIDIA=1
#
# Each package array stays rendered unconditionally so the rendered-script
# shellcheck job covers every package list on every host.
#
# facts-validate.tmpl still fails on an unknown fact, a boolean compared to a
# value, or a `desktop.<typo>`. A gate name cannot quietly mean nothing to the
# installer.
#
# FORCE_NVIDIA is NOT a fact and is NOT a
# gate: it is an env-var escape hatch layered on top of the seeded flag
# inside the installer (so a GPU-less test container can still exercise the
# driver repos and package set). See the closing note in
# .chezmoidata/facts.yaml for why it must never move into the shared partial.
packages:
  schemaVersion: 2
  linux:
    # ------------------------------------------------------------------------
    # Distro-independent shared package lists. flatpaks and dotnetTools are NOT
    # a distro concern: Flatpak apps come from Flathub (`flatpak install`)
    # and .NET global tools from `dotnet tool install -g` — both distro-agnostic
    # delivery mechanisms. They live HERE, as direct siblings of the
    # fedora: map (not nested under it), and the installer reads these
    # shared keys (.packages.linux.flatpaks / .packages.linux.dotnetTools).
    # Do NOT re-add flatpaks:/dotnetTools: under fedora: — the installer
    # now reads only these shared keys, so a per-distro copy would be silently
    # ignored (install nothing) rather than merged.
    #
    # Safe as list-typed siblings of the distro sub-map because NO consumer
    # iterates the key SET of .packages.linux — the installer and
    # dot_local/bin/executable_host-facts.tmpl both index it by explicit distro
    # name (`.packages.linux.fedora.*`, `index .packages.linux $distro`) — so
    # extra siblings here are inert to every reader.

    # Flatpak apps installed system-wide from Flathub via `flatpak install`.
    flatpaks:
      - com.discordapp.Discord
      - org.telegram.desktop
      - io.github.nroduit.Weasis
    # Global .NET tools installed via `dotnet tool install -g`.
    dotnetTools:
      - git-credential-manager
      - powershell
      - csharp-ls

    fedora:
      # Conditional package groups → the fact that gates each. Grammar and the
      # declaration-not-a-switch rule are in the file header above; every other
      # key under `fedora:` installs unconditionally.
      gates:
        # An NVIDIA GPU is present (PCI vendor 0x10de).
        nvidiaPackages: nvidia
        # Fedora KDE Spin (plasmashell shipped by the ISO).
        kdePackages: desktop.kde
        # Fedora Workstation (gnome-shell shipped by the ISO).
        gnomePackages: desktop.gnome
        # Neither a VM nor a container.
        bareMetalPackages: "!virt"
      # COPR (community) repositories enabled via `dnf copr enable`. Fedora-only
      # mechanism: `copr enable <owner/project>` resolves the whole repo from one
      # token.
      coprs:
        - jdxcode/mise
      # GPG keys imported (rpm --import) to verify third-party repo packages.
      # Fedora-only mechanism: rpm has ONE global keyring, so a key is declared once
      # and stands apart from the repos that use it.
      keys:
        # 1Password package signing key.
        - https://downloads.1password.com/linux/keys/1password.asc
        # VSCodium repository signing key.
        - https://gitlab.com/paulcarroty/vscodium-deb-rpm-repo/raw/master/pub.gpg
        # Google Chrome repository signing key.
        - https://dl.google.com/linux/linux_signing_key.pub
        # Cider Collective repository signing key.
        - https://repo.cider.sh/RPM-GPG-KEY
        # Microsoft 2025 signing key (Azure CLI RHEL 10 prod repo).
        - https://packages.microsoft.com/keys/microsoft-2025.asc
      # Third-party .repo definitions written verbatim to /etc/yum.repos.d.
      # Named for the artifact, like directRpms, because the entry SHAPE is the
      # mechanism: dnf reads a whole .repo ini (so the entry is one `content`
      # blob, gpgkey and all).
      dnfRepos:
        # 1Password desktop app + CLI stable RPM repo
        - id: 1password
          content: |
            [1password]
            name=1Password Stable Channel
            baseurl=https://downloads.1password.com/linux/rpm/stable/$basearch
            enabled=1
            gpgcheck=1
            repo_gpgcheck=1
            gpgkey="https://downloads.1password.com/linux/keys/1password.asc"
        # VSCodium (telemetry-free VS Code build) RPM repo
        - id: vscodium
          content: |
            [gitlab.com_paulcarroty_vscodium_repo]
            name=gitlab.com_paulcarroty_vscodium_repo
            baseurl=https://paulcarroty.gitlab.io/vscodium-deb-rpm-repo/rpms/
            enabled=1
            gpgcheck=1
            repo_gpgcheck=1
            gpgkey=https://gitlab.com/paulcarroty/vscodium-deb-rpm-repo/raw/master/pub.gpg
            metadata_expire=1h
        # Google Chrome stable RPM repo (x86_64 only; Google ships no other arch)
        - id: google-chrome
          content: |
            [google-chrome]
            name=google-chrome
            baseurl=https://dl.google.com/linux/chrome/rpm/stable/x86_64
            enabled=1
            gpgcheck=1
            gpgkey=https://dl.google.com/linux/linux_signing_key.pub
        # Cider (Apple Music client) RPM repo (x86_64 only; Cider ships no other arch)
        - id: cider
          content: |
            [cidercollective]
            name=Cider Collective Repository
            baseurl=https://repo.cider.sh/rpm/RPMS
            enabled=1
            gpgcheck=1
            gpgkey=https://repo.cider.sh/RPM-GPG-KEY
        # Azure CLI — Microsoft RHEL 10 prod repo. Chosen over the retired
        # yumrepos/azure-cli repo (el7-only, stale at 2.38.2): this repo serves
        # current azure-cli for x86_64 and aarch64, signed with the 2025 key.
        - id: azure-cli
          content: |
            [packages-microsoft-com-prod]
            name=packages-microsoft-com-prod
            baseurl=https://packages.microsoft.com/rhel/10/prod/
            enabled=1
            gpgcheck=1
            gpgkey=https://packages.microsoft.com/keys/microsoft-2025.asc
      # Remote .repo files added via `dnf config-manager addrepo`. Fedora-only
      # mechanism: dnf can fetch a vendor-authored .repo file whole and adopt it.
      repoFiles:
        - https://pkgs.tailscale.com/stable/fedora/tailscale.repo
      # Fedora enables RPM Fusion for Steam and other nonfree packages while the
      # CUDA repository owns the NVIDIA driver stack below. These package-name
      # globs become repository-scoped DNF exclusions so RPM Fusion cannot offer
      # its incompatible driver layout into the same transaction.
      nvidiaRpmFusionExcludes:
        - akmod-nvidia*
        - kmod-nvidia*
        - nvidia-modprobe
        - nvidia-persistenced
        - nvidia-settings
        - nvidia-xconfig
        - xorg-x11-drv-nvidia*
      # dnf groups installed via `dnf group install` (own transaction, separate
      # from the individual packages below). Fedora-only mechanism: dnf comps groups
      # are first-class install targets.
      groups:
        - development-tools
        - c-development
      # Foundational packages installed in their own transaction before the main
      # set, so kernel headers/devel are in place when the DKMS packages
      # (kmod-nvidia-latest-dkms, dkms) build their modules.
      corePackages:
        - clang21-libs
        - kernel
        - kernel-devel
        - kernel-devel-matched
        - kernel-headers
      packages:
        # Build tooling
        - clang
        - cmake
        - gcc-c++
        - gtk3-devel
        - java-latest-openjdk-devel
        - mesa-libGLU
        - ninja-build
        - pkg-config
        - systemd-devel
        - clang-tools-extra
        - mingw64-gcc
        # CLI utilities
        - btop
        - chezmoi
        - fd-find
        - jq
        - yq
        - gnupg2
        # kdotool: managed by mise (cargo:kdotool — see dot_config/mise/config.toml)
        - nvtop
        - ripgrep
        - tmux
        - xxd
        - yp-tools

        # Cloud provider CLI
        - azure-cli

        # Language toolchains + version manager
        - dotnet-sdk-10.0
        - dotnet-sdk-8.0
        - mise

        # Ruby build dependencies (consumed by mise's ruby-build).
        # The runtime libs libffi/libyaml are pulled in transitively.
        - libffi-devel
        - libyaml-devel

        # Hardware sensors / fan control
        - lm_sensors

        # secret-tool: reads the WakaTime API key that ~/.wakatime.cfg no longer
        # stores. The backing daemon is the desktop's own (ksecretd / gnome-keyring).
        - libsecret

        # Smartcard / code signing — Authenticode signing with the certificate held on
        # a SafeNet eToken 5110 (Gemalto IDPrime 940). The signing packages form one
        # chain — osslsigncode -> pkcs11-provider -> opensc -> pcscd -> CCID -> card —
        # and dropping any of them breaks signing; pcsc-tools is diagnostics only.
        - pcsc-lite
        - pcsc-lite-ccid
        - opensc
        - pkcs11-provider
        - osslsigncode
        - pcsc-tools

        # Password manager
        - 1password
        - 1password-cli

        # Editor
        - codium

        # Web browser
        - google-chrome-stable

        # Music player (Apple Music client; requires a purchased license)
        - Cider

        # Container runtime (rootless Podman + tooling)
        - podman
        - podman-docker
        - podman-compose
        - buildah
        - containers-common
        - passt
        - fuse-overlayfs
        - slirp4netns
        - kubernetes-client

        # Screen recording / streaming / video editing
        - kdenlive
        - obs-studio

        - pipewire-codec-aptx

        # Mesh networking
        - tailscale

        # Wayland clipboard management
        - wl-clipboard

        # Filesystem access and OS-level containers. No hypervisor is managed here.
        # fuse3 and ntfs-3g provide read-only ReFS/NTFS access to Windows Dev
        # Drive VHDX images. fuse3-devel supplies the refs-fuse / refsprogs
        # build dependency.
        - fuse3
        - fuse3-devel
        - ntfs-3g
        - systemd-container

        # Dynamic Kernel Module Support
        - dkms

        # Tauri development
        - curl
        - file
        - libappindicator-gtk3-devel
        - librsvg2-devel
        - libxdo-devel
        - openssl-devel
        - webkit2gtk4.1-devel
        - wget

        - expect
      # Gate `desktop.kde` — Fedora KDE Spin. fcitx5 is the unified input-method
      # framework across both KDE and GNOME (see gnomePackages for the same set
      # under its own gate; the two are mutually exclusive so the packages
      # cannot double-install). The KDE gate additionally pulls the Qt pinentry
      # KDE hosts reference from ~/.gnupg/gpg-agent.conf.
      kdePackages:
        - fcitx5
        - fcitx5-hangul
        - pinentry-qt
        # kitty is NOT installed from the distro package any more: the managed
        # config's vertical tab bar needs kitty >= 0.48 and the Fedora stream
        # skipped 0.44-0.46 and has not submitted 0.48. It is provisioned from
        # the locked upstream bundle by
        # .chezmoiscripts/00-tools/run_onchange_before_kitty.sh instead.
        # ONE-TIME MANUAL STEP on a host that still carries the distro package
        # (this repo adds no teardown scripts, and this manifest has no removal
        # key): sudo dnf remove kitty
        - xdg-terminal-exec
      # Gate `desktop.gnome` — Fedora Workstation. GNOME uses the SAME fcitx5
      # framework as the KDE targets (unified input-method policy: ibus is
      # rejected as too buggy). GNOME has no built-in KWin virtualkeyboard, so
      # fcitx5 autostarts from a per-user XDG autostart entry
      # (dot_config/autostart/org.fcitx.Fcitx5.desktop, NotShowIn=KDE), the
      # ibus source is REMOVED from org.gnome.desktop.input-sources on existing
      # hosts, and Kimpanel is installed so the candidate popup renders inside
      # GNOME Shell (mutter has no wlr-layer-shell) — see gnome.yaml
      # shellExtensions.kimpanel and .chezmoiscripts/50-linux-gnome/.
      gnomePackages:
        - fcitx5
        - fcitx5-hangul
        - pinentry-gnome3
        # kitty: provisioned from the locked upstream bundle — see the note in
        # kdePackages above, including the one-time `sudo dnf remove kitty`.
        - xdg-terminal-exec
      # Gate `!virt` — installed only when the host is neither a VM nor a
      # container (bare `systemd-detect-virt`, which is what this installer has
      # always probed; NOT the /etc manifest's `vm` fact, which is --vm).
      bareMetalPackages:
        - steam
      # Gate `nvidia` — a DISCRETE NVIDIA GPU is present (PCI vendor id 0x10de).
      # Pulled from the CUDA + nvidia-container repos added during repo setup,
      # under the same gate.
      #
      # A Jetson never reaches this group, and that is deliberate: this group is
      # under `fedora:` and only runs on Fedora. The board gets CUDA from the
      # `nvidia-jetpack` BSP metapackage instead (see the `nvidiaCompute` capability
      # below), and NVIDIA's own Thor setup guide forbids a generic `nvidia-cuda-toolkit`
      # on a Jetson because it does not match the L4T drivers the BSP ships.
      nvidiaPackages:
        - cuda-drivers
        - cuda-toolkit-13-3
        - kmod-nvidia-latest-dkms
        - nvidia-driver
        - nvidia-container-toolkit
        - nvidia-container-toolkit-base
        - libnvidia-container-tools
        - libnvidia-container1
      # RPMs installed directly from a vendor URL (no repo): import the GPG
      # key(s), then install the link matching the host arch (skipped when `id`
      # is already an installed package). An entry with a pinned `version` is
      # instead converged to that exact rpm VERSION (bumping the pin re-runs
      # `dnf install` as an in-place upgrade). Named for the artifact because
      # the artifact IS the mechanism.
      # The `rpms:` map is keyed by `uname -m` output (x86_64, aarch64) — those are
      # kernel arch identifiers matched verbatim by the installer, not schema keys,
      # so they keep their upstream spelling (see the exceptions note in the header).
      directRpms:
        - id: teamviewer
          keys:
            # TeamViewer 2017 package signing key.
            - https://download.teamviewer.com/download/linux/signature/TeamViewer2017.asc
            # TeamViewer current signing key.
            - https://linux.teamviewer.com/pubkey/currentkey.asc
          rpms:
            # 64-bit Intel/AMD.
            x86_64: https://download.teamviewer.com/download/linux/teamviewer.x86_64.rpm
            # 64-bit ARM.
            aarch64: https://download.teamviewer.com/download/linux/teamviewer.aarch64.rpm

  # Package authority v2. The Fedora installer continues to consume the legacy
  # lists above; this inventory is the validated cross-platform ownership ledger
  # that U5's native emitters will consume. Architecture names are normalized to
  # the values chezmoi exposes rather than manager-specific spellings.
  authority:
    supportedArchitectures:
      fedora: [amd64, arm64]
      macos: [arm64]
      ubuntu: [arm64]
    releaseGates:
      g0: closed
      g1: closed
    sources:
      - name: fedora
        backend: dnf
        origin: https://mirrors.fedoraproject.org/metalink
        trusted: true
      - name: onePassword
        backend: dnf
        origin: https://downloads.1password.com/linux/rpm/stable/$basearch
        trusted: true
      - name: onePasswordApt
        backend: apt
        origin: https://downloads.1password.com/linux/debian/arm64
        trusted: true
      - name: ubuntu
        backend: apt
        origin: http://ports.ubuntu.com/ubuntu-ports
        trusted: true
      - name: nvidiaJetson
        backend: apt
        origin: https://repo.download.nvidia.com/jetson
        trusted: true
      - name: vscodium
        backend: dnf
        origin: https://paulcarroty.gitlab.io/vscodium-deb-rpm-repo/rpms/
        trusted: true
      - name: googleChrome
        backend: dnf
        origin: https://dl.google.com/linux/chrome/rpm/stable/x86_64
        trusted: true
      - name: cider
        backend: dnf
        origin: https://repo.cider.sh/rpm/RPMS
        trusted: true
      - name: microsoftEdge
        backend: dnf
        origin: https://packages.microsoft.com/yumrepos/edge
        trusted: true
      - name: microsoftProd
        backend: dnf
        origin: https://packages.microsoft.com/rhel/10/prod
        trusted: true
      - name: flathub
        backend: flatpak
        origin: https://dl.flathub.org/repo/flathub.flatpakrepo
        trusted: true
      - name: homebrewCore
        backend: homebrew
        origin: https://github.com/Homebrew/homebrew-core.git
        trusted: true
      - name: homebrewCask
        backend: homebrew
        origin: https://github.com/Homebrew/homebrew-cask.git
        trusted: true

    # A capability has exactly one delivery owner on each OS. Non-managed
    # dispositions still carry complete architecture coverage so absence cannot
    # silently masquerade as support.
    capabilities:
      - id: onePasswordDesktop
        sentinel: 1password
        bootstrap: true
        integrity: signature
        fedora: { disposition: managed, owner: dnf, identifier: 1password, source: onePassword, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: 1password, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: managed, owner: releaseLock, identifier: 1password, architectures: { arm64: native } }
      - id: onePasswordCli
        sentinel: op
        bootstrap: true
        fedora: { disposition: managed, owner: dnf, identifier: 1password-cli, source: onePassword, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: 1password-cli, source: homebrewCask, elevation: none, greedy: false, architectures: { arm64: native } }
        ubuntu: { disposition: managed, owner: apt, identifier: 1password-cli, source: onePasswordApt, architectures: { arm64: native } }
      - id: gitTrimUbuntuOpenSslBuild
        fedora: { disposition: notApplicable, reason: This Ubuntu-specific git-trim build dependency is not managed on Fedora, architectures: { amd64: notApplicable, arm64: notApplicable } }
        macos: { disposition: notApplicable, reason: This Ubuntu-specific git-trim build dependency is not managed on macOS, architectures: { arm64: notApplicable } }
        ubuntu: { disposition: managed, owner: apt, identifier: libssl-dev, source: ubuntu, architectures: { arm64: native } }
      - id: gitTrimUbuntuDbusBuild
        fedora: { disposition: notApplicable, reason: This Ubuntu-specific git-trim build dependency is not managed on Fedora, architectures: { amd64: notApplicable, arm64: notApplicable } }
        macos: { disposition: notApplicable, reason: This Ubuntu-specific git-trim build dependency is not managed on macOS, architectures: { arm64: notApplicable } }
        ubuntu: { disposition: managed, owner: apt, identifier: libdbus-1-dev, source: ubuntu, architectures: { arm64: native } }
      - id: gpgSuite
        sentinel: gpg
        fedora: { disposition: managed, owner: dnf, identifier: gnupg2, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: formula, identifier: gnupg, source: homebrewCore, elevation: none, greedy: false, architectures: { arm64: native } }
        ubuntu: { disposition: managed, owner: apt, identifier: gnupg, source: ubuntu, architectures: { arm64: native } }
      - id: jq
        sentinel: jq
        fedora: { disposition: managed, owner: dnf, identifier: jq, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: formula, identifier: jq, source: homebrewCore, elevation: none, greedy: false, architectures: { arm64: native } }
        ubuntu: { disposition: managed, owner: apt, identifier: jq, source: ubuntu, architectures: { arm64: native } }
      - id: chrome
        sentinel: google-chrome
        gateClosing: g0
        fedora: { disposition: managed, owner: dnf, identifier: google-chrome-stable, source: googleChrome, architectures: { amd64: native, arm64: unresolved } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: google-chrome, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Thor does not use Chrome and KTD9 rejects a Flatpak or Snap fallback, architectures: { arm64: notApplicable } }
      - id: cider
        sentinel: cider
        gateClosing: g0
        fedora: { disposition: managed, owner: dnf, identifier: Cider, source: cider, architectures: { amd64: native, arm64: unresolved } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: cider, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Thor does not use Cider and KTD9 rejects a Flatpak or Snap fallback, architectures: { arm64: notApplicable } }
      - id: edge
        sentinel: microsoft-edge
        gateClosing: g0
        fedora: { disposition: managed, owner: dnf, identifier: microsoft-edge-stable, source: microsoftEdge, architectures: { amd64: native, arm64: unresolved } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: microsoft-edge, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Thor does not use Edge and KTD9 rejects a Flatpak or Snap fallback, architectures: { arm64: notApplicable } }
      - id: vscodium
        sentinel: codium
        fedora: { disposition: managed, owner: dnf, identifier: codium, source: vscodium, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: vscodium, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Thor does not use VSCodium and KTD9 rejects a Flatpak or Snap fallback, architectures: { arm64: notApplicable } }
      - id: githubCli
        sentinel: gh
        integrity: checksum
        fedora: { disposition: managed, owner: external, identifier: gh, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: external, identifier: gh, architectures: { arm64: native } }
        ubuntu: { disposition: managed, owner: apt, identifier: gh, source: ubuntu, architectures: { arm64: native } }
      - id: powershell
        sentinel: pwsh
        fedora: { disposition: managed, owner: dotnet, identifier: powershell, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: powershell, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Thor has no PowerShell workload and KTD9 rejects a Flatpak or Snap fallback, architectures: { arm64: notApplicable } }
      - id: azureCli
        sentinel: az
        fedora: { disposition: managed, owner: dnf, identifier: azure-cli, source: microsoftProd, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: formula, identifier: azure-cli, source: homebrewCore, elevation: none, greedy: false, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Azure CLI is not deployed on Thor per host-owner direction; the packages.microsoft.com apt repo remains the documented path if this is revisited, architectures: { arm64: notApplicable } }
      - id: cocoapods
        sentinel: pod
        fedora: { disposition: notApplicable, reason: CocoaPods is specific to Apple platform builds on macOS, architectures: { amd64: notApplicable, arm64: notApplicable } }
        macos: { disposition: managed, owner: homebrew, kind: formula, identifier: cocoapods, source: homebrewCore, elevation: none, greedy: false, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: CocoaPods is specific to Apple platform builds on macOS, architectures: { arm64: notApplicable } }
      - id: openjdk
        sentinel: java
        fedora: { disposition: managed, owner: dnf, identifier: java-latest-openjdk-devel, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: formula, identifier: openjdk, source: homebrewCore, elevation: none, greedy: false, architectures: { arm64: native } }
        ubuntu: { disposition: managed, owner: apt, identifier: default-jdk, source: ubuntu, architectures: { arm64: native } }
      - id: discord
        sentinel: discord
        fedora: { disposition: managed, owner: flatpak, identifier: com.discordapp.Discord, source: flathub, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: discord, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Discord is not deployed on Thor and KTD9 rejects a Flatpak or Snap fallback, architectures: { arm64: notApplicable } }
      - id: telegram
        sentinel: telegram-desktop
        fedora: { disposition: managed, owner: flatpak, identifier: org.telegram.desktop, source: flathub, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: telegram, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Telegram is not deployed on Thor and KTD9 rejects a Flatpak or Snap fallback, architectures: { arm64: notApplicable } }
      - id: teamviewer
        sentinel: teamviewer
        fedora: { disposition: managed, owner: directRpm, identifier: teamviewer, integrity: signature, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: teamviewer, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Thor's shared-host support model does not authorize a TeamViewer daemon, architectures: { arm64: notApplicable } }
      - id: tailscale
        sentinel: tailscale
        fedora: { disposition: managed, owner: dnf, identifier: tailscale, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: tailscale-app, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Thor deliberately stays off the tailnet, architectures: { arm64: notApplicable } }
      - id: obsStudio
        sentinel: obs
        fedora: { disposition: managed, owner: dnf, identifier: obs-studio, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: obs, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Thor does not record or stream video and KTD9 rejects a Flatpak or Snap fallback, architectures: { arm64: notApplicable } }
      - id: containerRuntime
        sentinel: podman
        fedora: { disposition: managed, owner: dnf, identifier: podman, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: formula, identifier: podman, source: homebrewCore, elevation: none, greedy: false, architectures: { arm64: native } }
        ubuntu: { disposition: unresolved, reason: Rootless container-runtime support on Jetson remains unproven, architectures: { arm64: unresolved } }
      - id: steam
        sentinel: steam
        legacyGroup: bareMetalPackages
        gate: "!virt"
        fedora: { disposition: managed, owner: dnf, identifier: steam, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: steam, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Steam has no supported ARM64 delivery and Thor has no gaming role, architectures: { arm64: notApplicable } }
      - id: koreanInput
        sentinel: fcitx5
        fedora: { disposition: managed, owner: dnf, identifier: fcitx5-hangul, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: manual, reason: native macOS Korean input source is built in and added in System Settings > Keyboard > Input Sources; no package manager owns it, architectures: { arm64: manual } }
        ubuntu: { disposition: managed, owner: apt, identifier: fcitx5-hangul, source: ubuntu, architectures: { arm64: native } }
      - id: logitechGestures
        sentinel: openlogi
        integrity: checksum
        fedora: { disposition: managed, owner: releaseLock, identifier: openlogi, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: openlogi, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Thor has no Logitech peripherals to manage, architectures: { arm64: notApplicable } }
      - id: nvidiaCompute
        sentinel: nvidia-smi
        gate: jetson
        fedora: { disposition: managed, owner: dnf, identifier: cuda-drivers, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: notApplicable, reason: supported Apple silicon has no NVIDIA GPU, architectures: { arm64: notApplicable } }
        ubuntu: { disposition: managed, owner: apt, identifier: nvidia-jetpack, source: nvidiaJetson, architectures: { arm64: native } }
      - id: kittyTerminal
        sentinel: kitty
        integrity: checksum
        fedora: { disposition: managed, owner: releaseLock, identifier: kitty, architectures: { amd64: native, arm64: native } }
        macos: { disposition: unresolved, reason: retained inline-image outcome awaits WezTerm gate, architectures: { arm64: unresolved } }
        ubuntu: { disposition: unresolved, reason: Kitty's Ubuntu arm64 package-delivery path has not been proven on Thor, architectures: { arm64: unresolved } }
      # U8 native desktop/hardware completions. logitechGestures (Logi Options+,
      # manual profile) and koreanInput (native Windows/macOS IMEs, manual
      # prerequisite steps) above already carry their U8 dispositions; the rows
      # below close the remaining retained desktop-app, pinentry, and
      # default-app helper gaps. None of them close G0 — only chrome, cider,
      # and edge carry gateClosing: g0.
      - id: weasis
        sentinel: weasis
        fedora: { disposition: managed, owner: flatpak, identifier: io.github.nroduit.Weasis, source: flathub, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: weasis, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Thor does not use Weasis and KTD9 rejects a Flatpak or Snap fallback, architectures: { arm64: notApplicable } }
      - id: kdenlive
        sentinel: kdenlive
        fedora: { disposition: managed, owner: dnf, identifier: kdenlive, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: kdenlive, source: homebrewCask, elevation: appManagement, greedy: true, architectures: { arm64: native } }
        ubuntu: { disposition: notApplicable, reason: Thor does not edit video and KTD9 rejects a Flatpak or Snap fallback, architectures: { arm64: notApplicable } }
      # One GPG pinentry capability, per-OS delivery: Fedora KDE hosts install
      # pinentry-qt and GNOME hosts pinentry-gnome3 (gated legacy groups above;
      # private_dot_gnupg/.gpg-agent.linux.conf picks the matching program from
      # the desktop fact), macOS installs pinentry-mac and .gpg-agent.darwin.conf
      # names it, and Windows gets pinentry inside the Gpg4win bundle.
      - id: pinentry
        fedora: { disposition: managed, owner: dnf, identifier: pinentry-qt, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: formula, identifier: pinentry-mac, source: homebrewCore, elevation: none, greedy: false, architectures: { arm64: native } }
        ubuntu: { disposition: managed, owner: apt, identifier: pinentry-qt, source: ubuntu, architectures: { arm64: native } }
      - id: defaultApps
        sentinel: defaultbrowser
        fedora: { disposition: managed, owner: dnf, identifier: xdg-terminal-exec, source: fedora, architectures: { amd64: native, arm64: native } }
        macos: { disposition: managed, owner: homebrew, kind: formula, identifier: defaultbrowser, source: homebrewCore, elevation: none, greedy: false, architectures: { arm64: native } }
        ubuntu: { disposition: unresolved, reason: The Ubuntu default-app helper path has not been proven on Thor, architectures: { arm64: unresolved } }
      # G1 CANDIDATE — opt-in launch only. Kitty (kittyTerminal above) stays the
      # authoritative Fedora terminal and the default on every host until G1b
      # opens; nothing here may switch a default or remove a Kitty source.
      # Ownership is native-manager only: winget on Windows, Homebrew on macOS —
      # a release-lock entry beside those would duplicate authority, and the
      # Fedora RPM path has no lockable artifact today (see the reason below).
      - id: weztermTerminal
        sentinel: wezterm
        fedora: { disposition: unresolved, reason: "G1a closed: upstream stable 20240203-110809-5046fc22 predates Kitty Unicode placeholder support and publishes no RPM for the current Fedora release (newest is fedora39) and no aarch64 RPM at all; only an upstream stable RPM for the current Fedora release, installed through DNF, is acceptable", architectures: { amd64: unresolved, arm64: unresolved } }
        macos: { disposition: managed, owner: homebrew, kind: cask, identifier: wezterm, source: homebrewCask, elevation: appManagement, greedy: false, architectures: { arm64: native } }
        ubuntu: { disposition: unresolved, reason: WezTerm's Ubuntu arm64 package-delivery path has not been proven on Thor, architectures: { arm64: unresolved } }
