#!/usr/bin/env bash

set -euo pipefail

real_bin="$HOME/.codex/packages/standalone/releases/current/bin"
real_codex="$real_bin/codex"

if [[ ${1-} == exec ]]; then
  PATH="$real_bin:$PATH" exec tokscale headless codex "$@"
fi

exec "$real_codex" "$@"
