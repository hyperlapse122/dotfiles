{{- /* VSCodium user settings — Windows target (%APPDATA%\VSCodium\User\settings.json).
   The body is ONE shared template (.chezmoitemplates/vscodium-settings.json.tmpl) so the
   Linux XDG and macOS targets render byte-identical settings from the same source. */ -}}
{{- includeTemplate "vscodium-settings.json.tmpl" . -}}