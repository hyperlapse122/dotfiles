{
  "//": "Build-time typecheck for the unmanaged-repo-guard omp extension (plan R20). It lives here, not beside the plugin source, because everything under dot_local/ deploys to $HOME and a compiler config has no business in the runtime plugin tree. The workspace under packages/ owns the only @types/node in the repo.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2022"],
    "types": ["node"],
    "typeRoots": ["../packages/node_modules/@types"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "verbatimModuleSyntax": true,
    "skipLibCheck": true,
    "allowImportingTsExtensions": true,
    "noEmit": true
  },
  "include": ["../dot_local/share/omp-plugins/plugins/unmanaged-repo-guard/src"]
}
