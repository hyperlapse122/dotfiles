# Require packages to be at least 24h old before install.
# npm uses `before` as a publish-time cutoff.
before=24h

# pnpm supply-chain hardening: block versions newer than 24h.
minimumReleaseAge=1440
