- defaults:
    link:
      relink: true
      create: true
      force: true
  link:
    "~":
      glob: true
      path: zsh/.**
    "~/":
      glob: true
      path: mise/.default-**
    ~/.yarnrc.yml:
      path: .yarnrc.yml
    "~/.config/ghostty/config":
      if: '[ `uname` = Darwin ]'
      path: ghostty/config
      create: true
      force: true
    ~/.config/environment.d/:
      glob: true
      path: environment.d/**
    ~/.config/:
      glob: true
      path: dotconfig/*.conf
    ~/.config/fcitx5:
      path: dotconfig/fcitx5
    ~/.config/autostart:
      path: dotconfig/autostart
    ~/.config/containers/:
      glob: true
      path: dotconfig/containers/**
    ~/.gitconfig:
      path: git/posix.gitconfig
    ~/.config/mise/config.toml:
      glob: true
      path: mise/config.toml
    ~/.config/Code/User/:
      glob: true
      path: vscode/**
    ~/.gnupg/:
      if: '[ `uname` = Linux ]'
      glob: true
      path: gnupg/**
    # ~/.gnupg/:
    #   if: '[ `uname` = Darwin ]'
    #   glob: true
    #   path: gnupg-macos/**

# - shell:
#   - [git clone --recursive https://github.com/sorin-ionescu/prezto.git ~/.zprezto, Cloning Prezto]
    
- shell:
  - [git submodule update --init --recursive, Installing submodules]