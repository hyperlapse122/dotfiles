# Residual Review Findings — chore/resolve-repo-guard-review-residuals

Source: `ce-code-review` (`mode:agent`) on branch `chore/resolve-repo-guard-review-residuals`, head `82c177a`. Reviewers: correctness, security, reliability, testing, adversarial (in-process fallback — the cross-model codex peer could not start; no independent-model corroboration). Originating PR carries the fixes for issues #171-#177.

## Filed

- **P2 — verdict-cache hit drops fork-chain re-checks** (correctness-reviewer, conf 75, pre-existing). `dot_local/share/omp-plugins/plugins/unmanaged-repo-guard/src/probe.ts` — `probeOne`'s cache returns `parent: null` on both the new fast path and the pre-existing slow path, so a verdict-cache hit for a fork skips re-walking its fork parent for the rest of that verdict's TTL. Pre-existing (the slow path has the same shape); surfaced by this review because R5 added a second cache-hit site. Filed: [#178](https://github.com/hyperlapse122/dotfiles/issues/178).

## Recorded (advisory / out of this PR's scope)
- **P3 — `onToolCall` has no exception boundary; a thrown error blocks without an audit record** (adversarial-reviewer, conf 50, advisory). `dot_local/share/omp-plugins/plugins/unmanaged-repo-guard/src/index.ts` — omp fails a thrown handler closed (the call is blocked), but a throw from `resolveCandidates` or `prober.evaluate` is not a `BlockResult`, so neither the nominal-type gate nor the `block: true` grep catches it and no audit line is written. R4's "every block leaves a record" holds for the three coded return paths; the throw path is an accepted edge. Advisory — not filed.
- **U1 error-attribution test not added** (testing-reviewer). The plan named a U1 scenario — a deliberately broken render inside a shared helper fails under the calling test's own `fail` prefix — that was not implemented. The extraction is byte-identical, so error attribution is preserved by construction; the scenario is belt-and-braces. Recorded as a known follow-up; not filed separately.
- **U1 shellcheck target-list assertion has no committed mutation test** (testing-reviewer). Lane A mutation-tested the workflow's `found_required_lib` assertion locally (pointed it at an uncollected name, confirmed failure, restored) but no committed test reproduces that. A workflow-step mutation test is awkward to commit; the assertion itself is real and was verified. Recorded; not filed.

## Applied in this PR (not residual)

The P1/P2 findings that were fixed in commit `82c177a` (`fix(guard): apply code-review findings`): `toArgv` ANSI-C three-state model + regression test; audit-log `O_NOFOLLOW` + symlink-leaf refusal; guarded logger calls in the config-failure catch and the audit-append catch; real-runtime external `timeout` backstop; U7 pre-warm pass-through + `tools/call` assertions; honest rewording of the sequential-append test.

## Notes

- The adversarial pass ran in the same model family as the implementation (in-process fallback). Treat its agreement as single-family review, not independent corroboration.
- The fork-chain finding equally affects the pre-existing slow-path cache check, so it is pre-existing rather than introduced by this PR's R5 fast path. R5's scope (per the plan) was the subprocess-saving fast path preserving R16's identity binding, which it does; the fork-chain behavior is orthogonal.
