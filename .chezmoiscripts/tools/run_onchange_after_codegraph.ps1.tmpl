{{ if eq .chezmoi.os "windows" -}}

# .chezmoiscripts/tools/run_onchange_after_codegraph.ps1.tmpl
#
# Adds codegraph to user PATH (idempotent; effectively one-shot).
# Reference: https://github.com/colbymchenry/codegraph/blob/main/install.ps1
#
# onchange, no external dependency: the PATH entry is version-independent
# (…\codegraph\current\bin), so the trigger is this script's own content.

$ErrorActionPreference = 'Stop'
$installDir = Join-Path $env:LOCALAPPDATA 'codegraph'
$dest = Join-Path $installDir 'current'
$binDir = Join-Path $dest 'bin'

$userPath = [Environment]::GetEnvironmentVariable('Path', 'User')
if (($userPath -split ';') -notcontains $binDir) {
  [Environment]::SetEnvironmentVariable('Path', "$binDir;$userPath", 'User')
  Write-Host "Added $binDir to your PATH (restart your terminal to pick it up)."
}

{{ end -}}